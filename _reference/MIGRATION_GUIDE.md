# 🚚 GastroMap V2: Руководство по Миграции Дизайна

Это руководство поможет перенести визуальный стиль и контент в новый проект, не копируя старую запутанную логику.

## ✅ Что копировать (Safe List)

Вы можете безопасно скопировать следующие файлы и папки из старого проекта в новый:

### 1. Конфигурация Стилей (В корень проекта)
*   `tailwind.config.js` — Содержит вашу цветовую палитру, шрифты и настройки темы.
*   `postcss.config.js` — Настройки сборщика CSS.
*   `jsconfig.json` (или `tsconfig.json`) — Настройки путей (alias `@/`).

### 2. Глобальные Стили
*   `src/index.css` (или `src/App.css`) — Все глобальные стили Tailwind и импорты шрифтов.

### 3. UI Компоненты (Скелет Дизайна)
Копируйте папку **`src/components/ui` целиком**.
*   Это компоненты shadcn/ui (Button, Input, Card, Dialog и т.д.).
*   Они не содержат бизнес-логики, только визуал.

### 4. Ассеты
*   `src/assets/` — Логотипы, картинки, шрифты.

### 5. Утилиты
*   `src/lib/utils.js` — Функция `cn()` для стилей (обязательно нужна для UI компонентов).

---

## ⚠️ Как переносить Публичные Страницы

Не копируйте файлы страниц (`src/pages/*.jsx`) целиком, так как они могут содержать старые хуки и логику. Вместо этого:

1.  **Создайте новый файл**: `src/features/public/pages/HomePage.jsx`.
2.  **Скопируйте JSX**: Откройте старый `src/pages/Home.jsx` (или `Index.jsx`), скопируйте только то, что внутри `return (...)`.
3.  **Перенесите компоненты**: Если на главной есть секции (например `Hero.jsx`, `Features.jsx`), найдите их в старом проекте и перенесите в `src/features/public/components/`.
    *   Проверьте их импорты. Удалите всё лишнее (старые хуки, сложную логику).

---

## 🚫 Что НЕ копировать (Danger List)

*   ❌ `src/pages/Admin.jsx` — **Ни в коем случае!** Пишем с нуля.
*   ❌ `src/hooks/*` — Большинство старых хуков могут тянуть лишние зависимости. Лучше написать новые простые хуки по мере необходимости.
*   ❌ `src/api/*` — Старый адаптер Supabase слишком сложный для старта. Используйте простые вызовы Supabase SDK.
*   ❌ `src/context/*` — Если там была сложная логика auth/theme, лучше переписать на Zustand.

---

## 📋 Чек-лист Миграции

1.  [ ] Инициализировать новый проект Vite.
2.  [ ] Установить Tailwind CSS.
3.  [ ] Скопировать `tailwind.config.js` и `index.css`.
4.  [ ] Скопировать `src/lib/utils.js`.
5.  [ ] Скопировать `src/components/ui`.
6.  [ ] Убедиться, что `npm run dev` запускается и стили работают (добавьте кнопку для теста).
7.  [ ] Начать перенос главной страницы (HomePage).
