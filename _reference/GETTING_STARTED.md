# 🚀 На старт, внимание, марш! (GastroMap V2)

Следуйте этим шагам, чтобы создать новый, чистый проект.

## 1. Инициализация (В терминале)

Перейдите в папку, где вы хотите создать новый проект (не внутри старого!):

```bash
cd /Users/macbookair15/Desktop/Gastromap_gravity
# Создаем новый проект "gastromap-v2"
npm create vite@latest gastromap-v2 -- --template react
cd gastromap-v2
npm install
```

## 2. Установка зависимостей

Установите базовый набор библиотек (без лишнего мусора):

```bash
# Стили и UI
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# Shadcn UI (понадобится для иконок и утилит)
npm install class-variance-authority clsx tailwind-merge lucide-react

# Роутинг и запросы
npm install react-router-dom @tanstack/react-query

# Supabase
npm install @supabase/supabase-js
```

## 3. Перенос Дизайна (Copy-Paste)

Теперь откройте два окна Finder/Explorer: старый проект и новый.

1.  **Конфиги**:
    *   Скопируйте содержимое `tailwind.config.js` из старого в новый.
    *   Скопируйте `src/index.css` в новый `src/index.css`.

2.  **Утилиты**:
    *   Создайте папку `src/lib`.
    *   Скопируйте `src/lib/utils.js`.

3.  **UI Компоненты**:
    *   Скопируйте ВСЮ папку `src/components/ui` из старого проекта в `src/components/ui` нового.

4.  **Лендинг (Дизайн)**:
    *   Скопируйте папку `src/components/landing` из старого проекта в `src/features/public/components` (создайте структуру папок как в ARCHITECTURE_V2.md).

## 4. Первый Запуск

Создайте простую главную страницу в `src/App.jsx` для теста:

```jsx
import { Button } from "@/components/ui/button"

export default function App() {
  return (
    <div className="p-10">
      <h1 className="text-3xl font-bold underline mb-4">
        Hello GastroMap V2!
      </h1>
      <Button>Shadcn Button работает!</Button>
    </div>
  )
}
```

Запустите:
```bash
npm run dev
```

Если вы видите стилизованную кнопку — фундамент готов! Можно приступать к переносу `Home.jsx` и подключению Supabase.

---

**Все инструкции лежат в** `docs/v2_skeleton/`:
1.  `ARCHITECTURE_V2.md` - Как строить структуру.
2.  `DATABASE_V2.md` - Как создать таблицы.
3.  `MIGRATION_GUIDE.md` - Детали переноса файлов.
